# Usage Examples

Usage examples for the `aegon` CLI will be documented here once
the tool reaches a runnable state with history and replay commands.

Planned:

- `aegon watch` — live TUI dashboard (implemented)
- `aegon history` — list past sessions
- `aegon replay <session-id>` — replay a past session in the TUI
- `aegon status` — show the most recent session summary
